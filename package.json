{
  "name": "webengage-expo-push",
  "version": "1.0.0",
  "description": "Expo config plugin for integrating WebEngage Push Notifications into iOS apps, with support for configuration via app.json.",
  "main": "./app.plugin.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf build && tsc && cp -a support/serviceExtensionFiles build/support/ && cp -a support/contentExtensionFiles build/support/",
    "lint": "eslint . --ext .ts",
    "prepare": "yarn run build"
  },
  "license": "MIT",
  "homepage": "https://github.com/WebEngage/webengage-expo-push#readme",
  "devDependencies": {
    "@expo/config-plugins": "^10.0.3",
    "@expo/config-types": "^53.0.4",
    "@typescript-eslint/eslint-plugin": "^5.10.1",
    "@typescript-eslint/parser": "^5.10.1",
    "eslint": "^8.8.0",
    "expo-module-scripts": "^4.1.7",
    "readme-md-generator": "^1.0.0",
    "typescript": "^5.8.3",
    "expo-modules-core": "^1.0.0"
  },
  "dependencies": {
    "expo-modules-core": "^1.0.0"
  },
  "expo-module": {
    "platforms": [
      "ios"
    ]
  }
}
